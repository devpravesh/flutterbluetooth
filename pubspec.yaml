name: flutterbluetoothtesting
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # flutter_blue: ^0.8.0
  flutter_blue_plus: ^1.15.5
  flutter_bluetooth_serial: ^0.4.0
  get: ^4.6.5
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
